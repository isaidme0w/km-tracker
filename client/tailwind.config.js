/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  // Dark theme is toggled via the `dark` class on the <html> element.
  darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [],
}